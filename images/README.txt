Photos live here, one folder per adventure:  images/<slug>/
Resize to ~1600px on the long side before uploading (keeps the repo small and pages fast).
Name the card image cover.jpg. The "welcome" folder holds generated placeholders — delete it once real trips are up.
